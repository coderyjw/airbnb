import { createSlice } from "@reduxjs/toolkit";

const homeSlice = createSlice({
  name: "home",
  initialState: {
    messgae: 'hello'
  },
  reducers: {

  }
})

export default homeSlice.reducer
